#!/usr/bin/env sh

if [ "self-update" == "$1" ]; then
    docker pull mkenney/phpdoc:php5
    wget -nv -O $0 https://raw.githubusercontent.com/mkenney/docker-phpdoc/php5/bin/phpdoc
else
    docker run --rm -t -i -v $(pwd):/src:rw mkenney/phpdoc:php5 $@
fi
